"""Compatibility shim for the former live API probe.

The manual probe lives at ``scripts/check_api.py``.  This file remains so
copy-only local deployments replace older versions that made network calls at
test-discovery time.
"""
